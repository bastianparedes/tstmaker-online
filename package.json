{
  "name": "tstmaker-online",
  "scripts": {
    "dev": "turbo run dev",
    "install": "turbo run install"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^1.13.0"
  }
}
