{
  "name": "tstmaker-online",
  "scripts": {
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "test": "turbo run test",
    "yarn-install": "cd apps/frontend ; yarn install ; cd ../backend ; npm run python-install"
  },
  "workspaces": {
    "packages":[
      "apps/*"
    ],
    "nohoist": [
      "**"
    ]
  },
  "devDependencies": {
    "turbo": "^1.13.0"
  },
  "private": true
}
