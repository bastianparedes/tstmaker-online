{
  "name": "backend",
  "version": "1.0.0",
  "engines": {
    "node": "^18.x"
  },
  "scripts": {
    "dev": "python api/dev.py",
    "lint": "flake8 --append-config pyproject.toml",
    "lint:fix": "autopep8 .",
    "test": "pytest",
    "python-install": "pip install -r requirements.txt"
  }
}
